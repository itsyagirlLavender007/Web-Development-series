<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cocktail Generator</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Saira+Condensed:wght@100;200;300;400;500;600;700;800;900&display=swap');

        body::-webkit-scrollbar {
            width: 15px;
          }
           
          body::-webkit-scrollbar-track {
            box-shadow: inset 0 0 6px #fdf2f8;;
          }
           
          body::-webkit-scrollbar-thumb {
            background-color: #be185d;
            outline: 1px solid #f9a8d4;
          }

        .container-fluid{
            font-family: "Saira Condensed", sans-serif;
            font-weight: 800;
            font-style: normal;
            background-color: #f9a8d4;
            min-height: 100vh;
            color:#fdf2f8;
        }

        span{
            color: #be185d;
        }

        ul{
            list-style-type: square;
            padding: 0;
        }

        li{
            margin:5px 0;
        }

        button{
            background-color: #be185d;
            color:#fdf2f8;
        }
    </style>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <div class="container-fluid p-5">
        <p class="h1 text-center">RANDOM <span>COCKTAIL</span> GENERATOR</p>
        <p class="text-center h4 mt-3">Click on the button below to generate random cocktails!</p>
        <button class="d-grid mx-auto mt-4 p-2 px-4" type="submit" id="noAuth" onclick="window.location.href='/'" style="border-radius: 15px; border: 2px solid #fdf2f8; font-size: 22px;">Generate Cocktail</button>
        <img class="w-25 mt-5 rounded mx-auto d-block" src="<%= drink_image%>"/>
        <p class="h3 text-center mt-3">This is a <span><%= drink_name%></span></p>
        <p class="h3 mt-3"><span>Ingredients:</span></p>
            <ul class="h4 mt-3">
            <% ingredients.forEach(ingredient => { %>
                <li><%= ingredient %></li>
            <% }) %>
            </ul>
        <p class="h3 mt-3"><span>Measures:</span></p>
            <ul class="h4 mt-3">
            <% measures.forEach(measure => { %>
                <li><%= measure %></li>
            <% }) %>
            </ul>
        <p class="h3 mt-3"><span>Instructions:</span></p>
        <p class="h4"><%= drink_instr%></p>
    </div>
</body>
</html>
